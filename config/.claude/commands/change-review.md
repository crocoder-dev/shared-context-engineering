---
description: Start SCE plan authoring from a change request
allowed-tools: Task, Read, Glob, Grep, Edit, Write, Question, Skill
---

Compatibility alias for `change-to-plan`.

Use the `shared-context-plan` agent, then load and follow the `sce-plan-authoring` skill.

Input change request:
`$ARGUMENTS`
