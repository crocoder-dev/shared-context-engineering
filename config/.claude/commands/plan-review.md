---
description: Review an approved plan and prepare or run the next task
allowed-tools: Task, Read, Glob, Grep, Edit, Write, Question, Skill, Bash
---

Compatibility alias for `next-task`.

Use the `shared-context-code` agent, then load and follow `sce-plan-review`.

Input:
`$ARGUMENTS`
